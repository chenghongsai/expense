# ---- PostgreSQL (Docker) ----
PGUSER=contoso
PGPASSWORD=contoso123
PGDATABASE=contoso_expense
PGHOST=localhost
PGPORT=5432

# SQLAlchemy 连接串（默认指向上面的 Docker 数据库）
DATABASE_URL=postgresql+asyncpg://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# ---- Auth ----
JWT_SECRET=change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ---- Seed 默认管理员（首次启动自动创建一个雇主账号）----
DEFAULT_ADMIN_EMAIL=admin@contoso.com
DEFAULT_ADMIN_PASSWORD=admin123
